

```
配置 maven
1 windows——Preferences——搜索maven——最下面的User Settings——将Global Settings指向maven路径中的conf目录下的settings.xml文件

2 windows -- preferences java installed jres 

3 创建maven工程：新建工程，选择other——搜索Maven Project——下一步——搜索webapp，选中并下一步——在这里Group ID是唯一标识，自己取一个，Artifact ID是项目名称，之后将Package清空，就不会默认创建包——点击Finish


项目 properties 

windows  preferences server runtime environments 


windows showview servers



```